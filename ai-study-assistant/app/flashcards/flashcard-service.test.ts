import {describe,expect,it} from "vitest";import {due,schedule} from "./flashcard-service";
const card={id:"1",front:"Q",back:"A",interval:1,ease:2.5,due_at:new Date(0).toISOString(),reviews:0,state:"new",created_at:new Date(0).toISOString()};
describe("flashcard scheduling",()=>{it("moves a new card into learning and schedules it",()=>{const n=schedule(card,"good");expect(n.reviews).toBe(1);expect(n.state).toBe("learning");expect(n.interval).toBe(1);expect(new Date(n.due_at).getTime()).toBeGreaterThan(Date.now()-1000)});it("moves reviewed cards into review state",()=>{const n=schedule({...card,reviews:1,interval:2},"easy");expect(n.reviews).toBe(2);expect(n.state).toBe("review");expect(n.interval).toBeGreaterThan(2)});it("makes hard reviews shorter than easy reviews",()=>{const hard=schedule({...card,reviews:2,interval:10},"hard");const easy=schedule({...card,reviews:2,interval:10},"easy");expect(hard.interval).toBeLessThan(easy.interval);expect(hard.ease).toBeLessThan(card.ease)});it("finds cards due now",()=>{expect(due([card])).toHaveLength(1);expect(due([{...card,due_at:new Date(Date.now()+86400000).toISOString()}])).toHaveLength(0)})});
