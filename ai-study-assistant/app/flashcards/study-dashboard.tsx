"use client";
import Link from "next/link";
import { useEffect, useMemo, useState } from "react";
import { readStorage } from "../lib/storage";
type Card={id:string;front:string;back:string;subject:string;topic:string};
const KEY="ai-study-assistant-flashcards";
const starter:Card[]=[{id:"1",front:"What is a variable?",back:"A symbol or name that represents a value that can change.",subject:"Mathematics",topic:"Algebra"},{id:"2",front:"What is Newton's first law?",back:"An object remains at rest or in uniform motion unless acted on by a net external force.",subject:"Science",topic:"Forces & Motion"}];
export default function StudyDashboard(){const[cards,setCards]=useState<Card[]>([]);const[subject,setSubject]=useState("All");useEffect(()=>setCards(readStorage<Card[]>(KEY,starter)),[]);const subjects=useMemo(()=>["All",...Array.from(new Set(cards.map(c=>c.subject)))],[cards]);const visible=subject==="All"?cards:cards.filter(c=>c.subject===subject);return <main className="min-h-screen bg-slate-950 text-white"><div className="mx-auto max-w-5xl px-5 py-8 sm:px-8 lg:px-10"><Link href="/flashcards" className="text-sm text-slate-400 hover:text-white">← Flashcards</Link><header className="mt-8"><p className="text-sm text-slate-500">Active recall</p><h1 className="mt-1 text-4xl font-bold">Study your cards</h1><p className="mt-3 text-slate-400">Choose a deck and review your saved flashcards.</p></header><div className="mt-8 flex flex-wrap gap-2">{subjects.map(s=><button key={s} onClick={()=>setSubject(s)} className={`rounded-xl px-4 py-2 text-sm font-semibold ${subject===s?"bg-white text-slate-950":"border border-slate-800 text-slate-400"}`}>{s}</button>)}</div><div className="mt-8 rounded-2xl border border-slate-800 bg-slate-900/60 p-6"><div className="flex items-center justify-between gap-4"><div><h2 className="text-xl font-bold">{subject} deck</h2><p className="mt-1 text-sm text-slate-500">{visible.length} cards available</p></div><Link href="/flashcards" className="rounded-xl bg-white px-5 py-3 text-sm font-semibold text-slate-950">Start studying</Link></div></div><div className="mt-6 grid gap-4 md:grid-cols-2">{visible.slice(0,6).map(card=><article key={card.id} className="rounded-2xl border border-slate-800 bg-slate-900/60 p-5"><p className="text-xs text-slate-500">{card.subject} · {card.topic}</p><h3 className="mt-3 font-semibold">{card.front}</h3></article>)}</div></div></main>}
