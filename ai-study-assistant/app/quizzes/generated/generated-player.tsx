"use client";
import { useMemo, useState } from "react";
import { QUIZ_HISTORY_KEY, QuizAttempt, readStorage, writeStorage } from "../../lib/storage";
type Question = { question: string; options: string[]; answer: number; subject?: string; topic?: string };
export default function GeneratedPlayer({ questions }: { questions: Question[] }) {
 const [current,setCurrent]=useState(0),[selected,setSelected]=useState<number|null>(null),[answers,setAnswers]=useState<(number|null)[]>(Array(questions.length).fill(null)),[finished,setFinished]=useState(false);
 const score=useMemo(()=>answers.reduce((t,a,i)=>t+(a===questions[i].answer?1:0),0),[answers,questions]); if(!questions.length)return null;
 if(finished)return <section className="rounded-2xl border border-slate-800 bg-slate-900/60 p-8 text-center"><p className="text-sm text-slate-500">AI quiz complete</p><h2 className="mt-2 text-3xl font-bold">{score}/{questions.length}</h2><p className="mt-3 text-slate-400">Your result has been saved to quiz history.</p></section>;
 const q=questions[current]; const choose=(i:number)=>{setSelected(i);setAnswers(a=>a.map((v,j)=>j===current?i:v));}; const next=()=>{if(current===questions.length-1){const h=readStorage<QuizAttempt[]>(QUIZ_HISTORY_KEY,[]);writeStorage(QUIZ_HISTORY_KEY,[{id:Date.now().toString(),title:`AI Quiz · ${q.topic||q.subject||"Study"}`,score,total:questions.length,date:new Date().toLocaleString()},...h]);setFinished(true);}else{setCurrent(current+1);setSelected(answers[current+1]);}};
 return <section className="rounded-2xl border border-slate-800 bg-slate-900/60 p-6 sm:p-8"><div className="flex justify-between text-sm text-slate-500"><span>Question {current+1} of {questions.length}</span><span>{q.topic||q.subject||"AI generated"}</span></div><div className="mt-3 h-2 rounded-full bg-slate-800"><div className="h-full rounded-full bg-white" style={{width:`${((current+1)/questions.length)*100}%`}}/></div><h2 className="mt-8 text-2xl font-bold leading-9">{q.question}</h2><div className="mt-6 grid gap-3">{q.options.map((o,i)=><button key={o} onClick={()=>choose(i)} className={`rounded-xl border px-4 py-4 text-left text-sm ${selected===i?"border-white bg-white text-slate-950":"border-slate-700 hover:border-slate-500"}`}>{String.fromCharCode(65+i)}. {o}</button>)}</div><div className="mt-7 flex justify-end"><button disabled={selected===null} onClick={next} className="rounded-xl bg-white px-6 py-3 text-sm font-semibold text-slate-950 disabled:opacity-30">{current===questions.length-1?"Finish quiz":"Next"}</button></div></section>;
}
