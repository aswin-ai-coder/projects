"use client";
import Link from "next/link";
import { useEffect,useMemo,useState } from "react";
import { readLocal,STORE,StudySessionRecord,QuizAttemptRecord } from "../lib/app-data";
export default function ProgressPage(){const[sessions,setSessions]=useState<StudySessionRecord[]>([]);const[attempts,setAttempts]=useState<QuizAttemptRecord[]>([]);useEffect(()=>{setSessions(readLocal(STORE.sessions,[]));setAttempts(readLocal(STORE.quizAttempts,[]))},[]);const total=sessions.reduce((n,s)=>n+s.minutes,0);const avg=attempts.length?Math.round(attempts.reduce((n,a)=>n+a.percent,0)/attempts.length):0;const days=useMemo(()=>new Set(sessions.map(s=>s.startedAt.slice(0,10))).size,[sessions]);return <main className="min-h-screen bg-slate-950 text-white"><div className="mx-auto max-w-5xl px-5 py-8"><Link href="/" className="text-sm text-slate-400 hover:text-white">← Dashboard</Link><header className="mt-8"><p className="text-sm text-slate-500">Learning analytics</p><h1 className="mt-1 text-4xl font-bold">Progress</h1><p className="mt-3 text-slate-400">See how consistently you are studying.</p></header><div className="mt-8 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{[["Study time",`${total} min`],["Quiz average",`${avg}%`],["Study days",days.toString()],["Sessions",sessions.length.toString()]].map(([a,b])=><div key={a} className="rounded-2xl border border-slate-800 bg-slate-900/60 p-5"><p className="text-sm text-slate-500">{a}</p><p className="mt-3 text-3xl font-bold">{b}</p></div>)}</div><section className="mt-8 rounded-2xl border border-slate-800 bg-slate-900/60 p-6"><h2 className="text-xl font-bold">Activity</h2><p className="mt-2 text-sm text-slate-400">Keep building consistent study days and completing quizzes to grow this dashboard.</p></section></div></main>}
