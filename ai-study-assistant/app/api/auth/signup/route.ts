import {NextResponse} from "next/server";import {createSession,createUser} from "../../../../server/auth";
export async function POST(req:Request){const b=await req.json().catch(()=>({}));const user=await createUser(String(b.email||""),String(b.password||""),String(b.displayName||""));if(!user)return NextResponse.json({error:"Use a valid email, name, and password of at least 8 characters."},{status:400});const s=createSession(user.id),r=NextResponse.json({user});r.cookies.set("study_session",s.id,{httpOnly:true,sameSite:"lax",secure:process.env.NODE_ENV==="production",expires:new Date(s.expires),path:"/"});return r}
