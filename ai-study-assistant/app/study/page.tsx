"use client";
import Link from "next/link";
import { useEffect, useState } from "react";
import { recordStudySession } from "../lib/storage";
export default function StudyPage(){const[minutes,setMinutes]=useState(25),[running,setRunning]=useState(false),[seconds,setSeconds]=useState(25*60),[subject,setSubject]=useState("Mathematics"),[topic,setTopic]=useState("Algebra");useEffect(()=>{if(!running)return;const id=setInterval(()=>setSeconds(s=>{if(s<=1){setRunning(false);recordStudySession(subject,topic,minutes);return minutes*60;}return s-1;}),1000);return()=>clearInterval(id)},[running,minutes,subject,topic]);const reset=()=>{setRunning(false);setSeconds(minutes*60)};const finish=()=>{recordStudySession(subject,topic,Math.max(1,Math.round((minutes*60-seconds)/60)));reset()};return <main className="min-h-screen bg-slate-950 text-white"><div className="mx-auto max-w-3xl px-5 py-8 sm:px-8 lg:px-10"><Link href="/" className="text-sm text-slate-400 hover:text-white">← Dashboard</Link><header className="mt-8"><p className="text-sm text-slate-500">Focused learning</p><h1 className="mt-1 text-4xl font-bold">Study session</h1><p className="mt-3 text-slate-400">Focus on one subject and save your session when you finish.</p></header><section className="mt-8 rounded-3xl border border-slate-800 bg-slate-900/60 p-8 text-center"><div className="grid gap-3 sm:grid-cols-2"><select value={subject} onChange={e=>setSubject(e.target.value)} className="rounded-xl border border-slate-700 bg-slate-950 px-4 py-3 text-sm"><option>Mathematics</option><option>Science</option><option>English</option></select><input value={topic} onChange={e=>setTopic(e.target.value)} placeholder="Topic" className="rounded-xl border border-slate-700 bg-slate-950 px-4 py-3 text-sm" /></div><p className="mt-10 font-mono text-6xl font-bold tabular-nums">{String(Math.floor(seconds/60)).padStart(2,"0")}:{String(seconds%60).padStart(2,"0")}</p><div className="mt-8 flex flex-wrap justify-center gap-2">{[15,25,45,60].map(n=><button key={n} onClick={()=>{setMinutes(n);setSeconds(n*60);setRunning(false)}} className={`rounded-xl px-4 py-2 text-sm ${minutes===n?"bg-white text-slate-950":"border border-slate-700 text-slate-400"}`}>{n} min</button>)}</div><div className="mt-6 flex justify-center gap-3"><button onClick={()=>setRunning(!running)} className="rounded-xl bg-white px-6 py-3 text-sm font-semibold text-slate-950">{running?"Pause":"Start"}</button><button onClick={finish} className="rounded-xl border border-slate-700 px-6 py-3 text-sm font-semibold">Finish & save</button><button onClick={reset} className="rounded-xl border border-slate-800 px-4 py-3 text-sm text-slate-500">Reset</button></div></section></div></main>}
